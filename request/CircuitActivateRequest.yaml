type: object
required:
  - contact
  - timeslot
  - idBuilding
  - serviceId
  - mcrId
  - slaId
  - customerId
  - streetNumber
  - organizationId
  - userId
  - dad
properties:
  contact:
    type: object
    required:
      - firstName
      - lastName
      - email
      - mobile_number
      - fixed_number
    properties:
      firstName: { type: string }
      lastName: { type: string }
      email: { type: string }
      mobile_number: { type: string }
      fixed_number: { type: string }
  timeslot: { type: integer }
  idBuilding: { type: string }
  serviceId: { type: integer }
  mcrId: { type: integer }
  slaId: { type: integer }
  customerId: { type: integer }
  streetNumber: { type: string }
  organizationId: { type: integer }
  userId: { type: integer }
  dad: { type: string }
  publicIpv6: { type: boolean }
  ipv4AdditionalId: { type: integer }
  ipv6AdditionalId: { type: integer }
  residentialContext: { type: string }
  stair: { type: string }
  floor: { type: string }
  unit: { type: string }
  headerId: { type: string }
