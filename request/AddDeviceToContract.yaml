type: array
items:
  type: object
  required:
    - deviceId
    - serialNumber
    - externalId
    - mrc
    - nrc
    - startDate
    - endDate
    - services
    - accessories
  properties:
    deviceId:
      { type: integer, description: "ID del tipo di device da associare" }
    serialNumber: { type: string, description: "Numero seriale del device" }
    externalId: { type: string, description: "ID esterno del device" }
    mrc: { type: number, format: float, description: "Monthly Recurring Cost" }
    nrc: { type: number, format: float, description: "Non-Recurring Cost" }
    startDate:
      { type: string, description: "Data di inizio (formato dd/MM/yyyy)" }
    endDate: { type: string, description: "Data di fine (formato dd/MM/yyyy)" }
    services:
      {
        type: array,
        items: { type: integer },
        description: "Array di ID dei servizi da associare al device",
      }
    accessories:
      {
        type: array,
        items: { type: integer },
        description: "Array di ID degli accessori da associare al device",
      }
